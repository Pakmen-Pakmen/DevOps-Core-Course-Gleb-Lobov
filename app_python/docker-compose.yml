services:
  app:
    build: .
    container_name: devops-info-app
    ports:
      - "5000:5000"
    environment:
      PORT: "5000"
      VISITS_FILE: "/data/visits"
    volumes:
      - ./data:/data
