Release {{ .Release.Name }} is installed.
{{- if contains "NodePort" .Values.service.type }}
Get the application URL by running:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "devops-info-chart.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else }}
Access with:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "devops-info-chart.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://127.0.0.1:8080/health
{{- end }}
